#ifndef PRIMES_H
#define PRIMES_H

#include <math.h>
#include <stdio.h>

#include "bitmap.h"

void print_primes_bitmap(size_t max_value);

void print_primes_bitmap2(size_t max_value);

#endif  // PRIMES_H